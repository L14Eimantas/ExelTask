@model ExelTask.Models.Workbook

@{
    ViewBag.Title = "DeleteUpload";
}

<h2>Delete Upload File</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.WorkbookName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.WorkbookName)
        </dd>
    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()
        <div class="form-actions no-color">
                @Html.ActionLink("Cancel", "UploadIndex", new {id= Model.MappingId}, new { @class = " btn btn-default" })
                <input type="submit" value="Delete" class="btn btn-default" />
        </div>
    }
</div>
